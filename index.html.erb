<!DOCTYPE html>
<html>
  <head>
    <title>RubyRobots</title>
    <style type="text/css">
      body {
        font-family: sans-serif;
      }
      #log {
        background-color: #f7f7f7;
        border: 1px solid #e0e0e0;
        padding: 1em;
      }
      #arena, #text {
        float: left;
      }
      #text {
        width: 390px;
        margin-right: 10px;
      }
      #arena svg {
        border: 1px solid #e0e0e0;
        background-image: url("tiles.png");
      }
      textarea {
        font-family: monospace;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <div id="arena"></div>
    <div id="text">
      <div>
      Help Ruby program her robots! Challenge 1 is the Dummy bot. Target practice!
      </div>
      <textarea cols="50" rows="20" id="brains">
class MyBot
  include Robot

  def tick(events)
    # Write your robot brains in here!
    fire 20 if robot_scanned?
    turn_gun 5
  end
end
      </textarea>
      <pre class='commands'>
Available commands:

fire &lt;strength> (i.e. `fire 1`)
turn_gun &lt;speed>  (i.e. `turn_gun 2`)

Available events:

- robot_scanned?

      </pre>
      <button id='run'>Battle!</button>

      <pre id="log">Battle report</pre>
    </div>
    <%= javascript_include_tag 'application' %>
  </body>
</html>
